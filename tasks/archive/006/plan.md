---
status: согласовано
---

# План TASK-006

## Декомпозиция

| ID | Цель / проблема | Решение | Проверка | Состояние |
|---|---|---|---|---|
| P01 | `durationMinutes` не ограничен — принимает отрицательные/абсурдно большие значения | Добавить `@minValue(1)` `@maxValue(1440)` в `EventType` и `CreateEventTypeRequest` (`event-type.tsp`) | `contracts:build` без ошибок, значение вне диапазона отклоняется generated Zod-схемой | завершено |
| P02 | `slotIntervalMinutes` не ограничен | Добавить `@minValue(15)` `@maxValue(60)` в `CalendarSettings`, `SetupRequest`, `CalendarSettingsResponse` (`owner.tsp`) | `contracts:build` без ошибок | завершено |
| P03 | `availabilityRules` не имеет минимального размера, хотя doc-комментарий уже требует «At least one rule» | Добавить `@minItems(1)` в те же три модели (`owner.tsp`) | `contracts:build` без ошибок, пустой массив отклоняется | завершено |
| P04 | `IanaTimeZone.pattern` отвергает валидные зоны (`UTC`, `America/Port-au-Prince`, `Etc/GMT+3`) | Заменить `^[A-Za-z]+/[A-Za-z_]+(/[A-Za-z_]+)?$` на `^[A-Za-z0-9+_-]+(/[A-Za-z0-9+_-]+){0,2}$` (`common.tsp`) | Ручная проверка регэкспа на наборе валидных/невалидных зон + `contracts:build` | завершено |
| P05 | `ErrorResponse.code` не ограничен по длине | Добавить `@maxLength(100)` (`errors.tsp`) | `contracts:build` без ошибок | завершено |
| P06 | Query-параметр `eventTypeId` в `getPublicSlots` — остаток риска S3 из `task-003`, не ограничен | Добавить `@maxLength(100)` на параметр (`operations/public.tsp`) | `contracts:build` без ошибок, OpenAPI параметр содержит `maxLength` | завершено |
| P07 | `getPublicSlots` не документирует 400 `ValidationError`, хотя `createPublicBooking` документирует | Добавить вариант ответа `400 ValidationError` в `getPublicSlots` (`operations/public.tsp`) | OpenAPI responses операции содержат `ValidationError` | завершено |
| P08 | `HealthResponse` определена в `operations/health.tsp`, а не в `models/common.tsp` — нарушает принятое разделение models/operations | Перенести модель в `common.tsp`, `health.tsp` только ссылается | `contracts:build` без ошибок, `HealthResponse` фигурирует в components.schemas как и раньше | завершено |
| P09 | `@service` не указывает версию, generated `info.version` = `0.0.0` | Указать `version: "0.1.0"` через `@info` в `main.tsp` | `info.version` в generated OpenAPI == `0.1.0` | завершено |
| P10 | Source + generated packages converge после P01–P09 | Прогнать полный pipeline: `contracts:build` → `generate` → `generate:check` → `typecheck --workspaces --if-present` | Все команды без ошибок, diff в generated отсутствует | завершено |
| P11 | Gate-скрипт `tests/contract-validation.test.ts` из `task-003` даёт ложный PASS: проверка `ownerId` не резолвит `$ref`, `yaml` не объявлен зависимостью, нет npm-скрипта `test`, `assert` падает на первой ошибке | Резолвить `$ref` перед проверкой полей на `ownerId`; добавить `yaml` в `dependencies` `package.json`; добавить `"test"` npm-скрипт; собрать все несоответствия в массив и вывести все перед завершением с ошибкой | `npm test` (новый скрипт) проходит на текущем контракте; намеренная порча одного поля в тестовом прогоне даёт `FAIL` (не вакуумный PASS) | завершено |
| P12 | `apps/api/src/server.ts` разошёлся с контрактом: `/health` возвращает лишнее `uptimeSeconds`, 404 не в форме `ErrorResponse` | Убрать `uptimeSeconds` из ответа `/health`; привести 404-ответ к форме `{ code, message }` | Ручной smoke-запрос `GET /health` и любой неизвестный маршрут возвращают тела, соответствующие `HealthResponse`/`ErrorResponse` | завершено |
| P13 | `README.md`: «OpenAPI 3.1» вместо фактических 3.0; нет пояснения про локальные `docs/`/`tasks/`/`.opencode/` | Исправить упоминание версии OpenAPI; добавить раздел о локальных, не публикуемых в git каталогах | Ручная проверка текста README | завершено |
| P14 | Итоговый gate — зафиксировать готовность | Заполнить `result.md`: что изменено, проверки, acceptance criteria, известные ограничения (невыразимые schema-инварианты из ADR) | `result.md` заполнен, итог явный | завершено |

## Порядок и зависимости

```text
P01 ─┐
P02 ─┤
P03 ─┤
P04 ─┼─→ P10 ─┐
P05 ─┤        │
P06 ─┤        │
P07 ─┤        │
P08 ─┤        │
P09 ─┘        │
P11 (независим от P01–P09, но проверяется на результате P10) ─┤
P12 (независим, синхронизация runtime) ─┼─→ P14
P13 (независим, документация) ─────────┘
```

P01–P09 — правки `.tsp`, могут вестись параллельно (разные модели/файлы), но P02/P03 оба меняют `owner.tsp` — согласовать порядок правок внутри файла вручную. P10 — единая точка конвергенции source/generated после всех правок контракта. P11/P12/P13 не зависят от порядка друг друга и от P01–P09 по содержанию, но P11 наиболее полезен после того, как P01–P09 применены (проверяет их результат). P14 — финальный, требует завершения всех предыдущих пунктов.

## Состояние пунктов и порядок выполнения

Реализация выполнена и запушена **до** согласования документов задачи — коммит `f029414` («feat: task-006 — contract constraints hardening and gate script fixes», ветка `task/006-contract-hardening`, 18 файлов). Это инверсия обычного порядка из `tasks/README.md`, где документы гейтят реализацию. Пункты P01–P14 помечены `завершено` по факту содержимого коммита и зафиксированных в нём проверок, а не по факту согласования.

Что это означает практически:

- фактическое состояние кода описано в [`result.md`](result.md) со ссылкой на коммит — начинать реализацию заново не нужно;
- согласование задачи получено от пользователя 2026-08-04, все четыре файла переведены в `status: согласовано`;
- если при согласовании brief или ADR выяснится, что часть реализованного не соответствует замыслу, расхождение фиксируется как дефект в `result.md`, а не переписывается молча.

## Блокеры и открытые вопросы

- Согласование получено: задача закрыта (2026-08-04, все четыре файла `status: согласовано`).
- Аудит AI-workflow позже нашёл, что P13 исправил упоминание «OpenAPI 3.1» только в `README.md`, тогда как идентичное утверждение оставалось в `docs/contract-pipeline.md`. Оно исправлено отдельно, вне этой задачи, — см. «Известные ограничения» в `result.md`.
