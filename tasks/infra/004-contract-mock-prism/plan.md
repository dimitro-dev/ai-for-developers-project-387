# План TASK-INFRA-004

## Декомпозиция

| ID | Цель / проблема | Решение | Проверка | Состояние |
|---|---|---|---|---|
| P01 | Нет инструмента mock: `@stoplight/prism-cli` не объявлен, версия не зафиксирована в lock | Добавить `@stoplight/prism-cli` в корневые `devDependencies` (явная зависимость, не из транзитивного резолва), версия зафиксируется в `package-lock.json` | `npm ls @stoplight/prism-cli` показывает зависимость, `package-lock.json` содержит версию; `npm ci` воспроизводим | завершено |
| P02 | Нет одной команды запуска мока | Добавить корневой npm-скрипт `"mock:prism": "prism mock packages/contracts/generated/openapi.yaml -p 4010"` | `npm run mock:prism` поднимает Prism на `http://localhost:4010` без ошибок | завершено |
| P03 | Нет подтверждения, что мок отвечает на все 11 операций контракта | Smoke-серия `curl` к каждому из 8 маршрутов (11 operationId), ответы `200`/2xx с телом по схеме | В `result.md` зафиксирована таблица маршрут → HTTP-статус | завершено |
| P04 | Мок должен отклонять невалидные запросы, а не отвечать дефолтным примером | Проверить поведение Prism по умолчанию (валидация включена): `POST /bookings` без обязательного `guest.email` и/или с `guest.email` вне `pattern` → 4xx (фактически `400` с `sl-violations`), а не `201` | `curl -X POST /bookings` с невалидным телом возвращает `400` (4xx), а не `201`; заголовок `sl-violations` фиксирует ошибку в `request` | завершено |
| P05 | Операции с несколькими error-ответами должны позволять запросить конкретный не-2xx статус | Штатный механизм Prism `Prefer: code=<status>`: `createPublicBooking` → `404`, `getPublicSlots` → `404`/`400` | `curl -H "Prefer: code=404"` возвращает запрошенный статус с телом из контракта | завершено |
| P06 | README не описывает мок: команда, порт, режим валидации, переключение клиента | Добавить в README секцию «Mock-сервер»: команда запуска, порт `4010` (документируется как занятый наряду с `3001`/`8081`), режим валидации (невалидный запрос → 4xx с `sl-violations`, фактически `400`), переключение base URL клиента через `client.setConfig({ baseUrl })` (`@minical/api-client`) | Ручная проверка текста README | завершено |
| P07 | Регрессия существующих команд после добавления зависимости и скрипта | Убедиться, что `npm run generate`, `npm run generate:check`, `npm run typecheck`, `npm run test`, `npm run build` проходят без изменений поведения | Все команды exit 0, generated diff отсутствует | завершено |
| P08 | Итоговый gate — зафиксировать готовность и acceptance criteria | Заполнить `result.md`: что изменено, таблица smoke-проверок 11 операций, проверки AC#1–#8, известные ограничения | `result.md` заполнен, каталог задач в `tasks/README.md` актуализирован | завершено |

## Порядок и зависимости

```text
P01 ──→ P02 ──→ P03 ─┐
                P04 ─┼─→ P07 ──→ P08
                P05 ─┤
P06 (независим, README) ──┘
```

P01 — единственная установка зависимости, всё остальное опирается на неё. P02 — скрипт запуска. P03–P05 — проверки поведения мока, не зависят друг от друга по порядку и могут выполняться в любой очерёдности. P06 — документация, независима. P07 — регресс-гейты после всех изменений. P08 — финальный, требует завершения P01–P07.

## Блокеры и открытые вопросы

- Нет. Согласование brief/adr/plan получено от пользователя 2026-08-04; реализация не меняет контракт (API impact `NONE`), поэтому Contract Agent не требуется.
